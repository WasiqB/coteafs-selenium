name: Selenium Grid Suite
tests:
  - name: Test Grid
    parameters: { test.browser: REMOTE, test.config: ./src/test/resources/config/grid/selenium-grid-config.yaml }
    classes:
      - name: com.github.wasiqb.coteafs.selenium.SeleniumTest
        includedMethods:
          - testLogin
